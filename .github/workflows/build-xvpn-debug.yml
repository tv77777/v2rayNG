name: Build XVPN Debug APK

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # إخفاء تحذيرات Node.js وتفعيل الإصدار الأحدث
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platforms;android-34 build-tools;34.0.0'

      - name: Install NDK
        run: |
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install "ndk;29.0.14206865"
          echo "NDK_HOME=$ANDROID_HOME/ndk/29.0.14206865" >> $GITHUB_ENV

      - name: Prepare Libs Directory
        run: mkdir -p ${{ github.workspace }}/V2rayNG/app/libs

      - name: Download libv2ray
        uses: robinraju/release-downloader@v1.11
        with:
          repository: '2dust/AndroidLibXrayLite'
          latest: true # تنزيل أحدث نسخة مستقرة لضمان عدم حدوث خطأ في الـ Tag
          fileName: 'libv2ray.aar'
          out-file-path: V2rayNG/app/libs/

      - name: Verify Library Download
        run: ls -lh ${{ github.workspace }}/V2rayNG/app/libs/

      - name: Build with Gradle
        run: |
          cd ${{ github.workspace }}/V2rayNG
          chmod +x gradlew
          # تمرير الإعدادات مباشرة لـ Gradle لضمان التوافق مع AGP 9.0
          ./gradlew clean assembleDebug \
            -Pandroid.ndkVersion="29.0.14206865" \
            -Pandroid.newDsl=false \
            --stacktrace

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: XVPN-Debug-APK
          path: |
            **/build/outputs/apk/**/*.apk
          if-no-files-found: error
